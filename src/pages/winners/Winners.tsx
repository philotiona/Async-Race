import type { ReactNode } from "react";
import styles from "./Winners.module.css"

export default function Winners(): ReactNode {
    return(
        <div className={styles.wrapper}>

        </div>
    )
}